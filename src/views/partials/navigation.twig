<nav class="navbar navbar-default navbar-custom navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}"><span class="glyphicon glyphicon-globe" aria-hidden="true"></span> Greenworld Energies</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if auth.check %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                           aria-haspopup="true" aria-expanded="false">{{ auth.user.getUsername }} <span
                                    class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path_for('logout') }}">Logout</a></li>
                        </ul>
                    </li>
                    {% if  auth.user.getRole == 'admin' %}
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                               aria-haspopup="true" aria-expanded="false">Usermanagement<span
                                        class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{ path_for('usertable') }}">Usertable</a></li>
                            </ul>
                        </li>
                    {% endif %}
                {% else %}
                    <li>
                        <a href="{{ path_for('/userservice/signIn') }}">
                            <i class="fa fa-address-card" aria-hidden="true"></i>
                            Kundenbereich
                        </a>
                    </li>
                    <li><a href="{{ path_for('/userservice/signUp') }}">Neuer Kunde</a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>